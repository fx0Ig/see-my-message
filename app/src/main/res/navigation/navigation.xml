<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/fragmentSplash">

    <fragment
        android:id="@+id/fragmentSplash"
        android:name="com.example.seemymessage.fragments.FragmentSplash"
        android:label="fragment_splash"
        tools:layout="@layout/fragment_splash" >
        <action
            android:id="@+id/action_fragmentSplash_to_fragmentMenu"
            app:destination="@id/fragmentMenu" />
    </fragment>
    <fragment
        android:id="@+id/fragmentMenu"
        android:name="com.example.seemymessage.fragments.FragmentMenu"
        android:label="fragment_menu"
        tools:layout="@layout/fragment_menu" >
        <action
            android:id="@+id/action_fragmentMenu_to_fragmentTextSettings"
            app:destination="@id/fragmentTextSettings" />
    </fragment>
    <fragment
        android:id="@+id/fragmentTextSettings"
        android:name="com.example.seemymessage.fragments.FragmentTextSettings"
        android:label="fragment_text_settings"
        tools:layout="@layout/fragment_text_settings" />
</navigation>